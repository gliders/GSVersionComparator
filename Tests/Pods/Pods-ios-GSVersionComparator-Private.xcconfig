#include "Pods-ios-GSVersionComparator.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/GSVersionComparator" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/GSVersionComparator"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}